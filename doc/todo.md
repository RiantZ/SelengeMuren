# TODO

## Next Sprint
- Добавление для логов модулей и их сериализация
- мониторинг потерь данных - мониторинг в логах, трейсах, метриках и регулярный пулл из core - создание статистики которую можно использовать в тестах
- тест производительности 
  - логов против принтф в файл из нескольких тредов, замер времени и места на диске
  - СДК от Датадога С++
  - СДК от Датадога для профайлинга?
- Создание презентации с цифрами (оттолкнуться от )
  - Анализ рынка и конкурентов
  - Потребности клиентов
  - Архитектура
  - Что мы предлагаем (p8, microP8, server, datadog integration)  
- Найдена нестабильность (flaky crash). На втором запросе «100 раз» один из прогонов упал: бинарник P8_RegressionTests был убит (subprocess killed, rc=8) на ~60-й итерации, вывод оборвался на тесте c_writer_registry_test.no_core_writer_is_noop.
Воспроизвести не удалось:
Изолированный прогон c_writer_registry_test.* — прошёл.
Полный набор в цикле 200 раз подряд — все 200 прошли, 0 падений (~110 сек).
Оценка. Это редкий недетерминированный краш (примерно 1 на 60–200+ прогонов). Тесты в этом наборе (concurrent_register_unregister, concurrent_register_unregister_churn, three_writers_per_thread)

## Backlog

- look at https://nemequ.github.io/hedley/api-reference.html for prediction marco



## Done
- add pre-hook for format & auto-install by cmake

Создание синков
- ответить на вопрос поддержки мульти-синков? **Нет, такую поддержку мы делать не будем**
- Базовая архитектура
 - синк создается ядром на основании конфига
 - в ядре есть нить которая потребляет данные которые поступают через release_buffer
 - в ядре есть сериализированные сервисные данные которые мы тоже будем отдавать в синк
    * модули
    * аттрибуты
    * нити
    * логи
    * метрики
    * трейсы
- Как правильно сериализировать сервисные данные? 
    - Ядро хранит сервисные данные в мапах и в отельных линейно сериализированных смешанных буферах
    - Ядро хранит инкрементальную версию сериализированных буферов и ссылку в буферах на предыдущую запрошенную версию
```    [{header}{payload}] ... [{header}{payload .......}]
                                                 ^ {Pointer, size} 
```
    - Синк хранит свою версию последних обновлений (pointer), при запросе сравнивают 2 указателя, если они не равны то 
      ядро отдает список буферов на запись
    - Я бы хотел чтоб синк при Reset (обрыв связи, новый файл) не записывал ВСЕ сервисные данные снова а только то что 
      было использовано в последних буферах данных посланных после Reset.
      Это сложная задача требующая либо
        - либо хранить на уровне каждого буфера все ID сервисных данных
        - либо сканировать каждый буфер на их предмет
      Это крайне затратно и тяжело реализуемо.
      Единственно что может смягчить сложно - будучи зарегест один раз - сервисные данные остаются иммутабельны
      Один из вариантов это предоставить синку самому решать как поступать с сервисными данными
        - если это файл то хранить сервисные данные в отельном постоянном файле
        - если это сеть? 
          Был разрыв связи и что мы делаем в этом случае? сохраняем на диск? удаленная сторона требует послать сервисные данные - мы шлем 

    - А если синк запрашивает только последние обновления
    - Синк хранит последнюю версию сервисных данных
    - А если синк запрашивает все данные с начала
- Создание базового синка для сохранения на диск
    - Синк создаетс
